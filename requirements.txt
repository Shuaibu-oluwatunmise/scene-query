# PyTorch — install separately with the right CUDA version:
#   https://pytorch.org/get-started/locally/
#   e.g. pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124

numpy>=2
einops
scipy>=1.11
opencv-python>=4.8
pillow>=10.0
tqdm>=4.65
requests

# Visualisation
rerun-sdk==0.32.1

# GroundingDINO requires transformers==4.40; newer versions removed get_head_mask from BertModel
transformers==4.40.0

# Model downloads
gdown
huggingface_hub
